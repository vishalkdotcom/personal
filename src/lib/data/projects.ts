// Migrated from Next.js data/projects.ts
// This will be updated with the actual project data during component migration

export interface Project {
	id: string;
	title: string;
	description: string;
	image: string;
	technologies: string[];
	link?: string;
	github?: string;
}

export const projects: Project[] = [
	// Project data will be migrated from the original projects.ts file
	// This is placeholder structure for the SvelteKit migration
];
# Markdown siblings: correct type if a request hits the asset instead of Functions.
/*.md
  Content-Type: text/markdown; charset=utf-8
  Vary: Accept

# Content negotiation is live on document routes (Functions also set this).
/*
  Vary: Accept
